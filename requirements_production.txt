# ═══════════════════════════════════════════════════════════════════════════════
#  PRODUCTION REQUIREMENTS FOR LUNA-EQUIVALENT S2S AI
#  Tested on: Python 3.10+, CUDA 12.1+, RunPod H200
# ═══════════════════════════════════════════════════════════════════════════════

# Core PyTorch (installed separately for CUDA compatibility)
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# Audio Processing
torchaudio>=2.0.0
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
audioread>=3.0.0

# Deep Learning
einops>=0.7.0
transformers>=4.35.0
accelerate>=0.25.0
safetensors>=0.4.0

# HuggingFace Datasets
datasets>=2.14.0
huggingface_hub>=0.19.0

# Numerical
numpy>=1.24.0
scipy>=1.11.0

# Progress & Logging
tqdm>=4.66.0
tensorboard>=2.15.0
wandb>=0.16.0

# Web Server (for inference)
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# Utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
colorama>=0.4.6
rich>=13.0.0

# Testing & Quality
pytest>=7.4.0
black>=23.0.0
isort>=5.12.0

# Optional: Flash Attention (install separately)
# pip install flash-attn --no-build-isolation

# Optional: Metrics
pesq>=0.0.4
pystoi>=0.3.3
